1. cd backend
2. Create a virtual environment and activate it:
   - For Windows:
     ```
     python -m venv venv
     .venv\Scripts\activate
     ```
   - For macOS/Linux:
     ```
     python3 -m venv venv
     source venv/bin/activate
     ```